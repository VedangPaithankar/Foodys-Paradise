# Copy to .env (gitignored, never commit) and fill in real values.

# One-time ETL source -- copy MONGO_URI by hand from the sibling project's
# own .env at:
#   E:\Coding - Projects\Foodys-Paradise\FoodysParadise - BackEnd\.env
MONGO_URI=

# Dockerized Postgres this service reads from (matches docker-compose.yml)
DATABASE_URL=postgresql://foodys_paradise:foodys_paradise@localhost:5435/foodys_paradise
